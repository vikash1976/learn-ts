import { mylib } from 'mylib';
//const _:myLodash = require('lodash');

import {_} from 'mylodash';





const colors = ['red', 'orange', 'blue', 'green'];
const fisrtColor = _.first(colors);
_.first(colors);

 const mylibReturn = mylib.aFunc(colors);

